"""
Unit tests for the feature pipeline (add as features are implemented).

PLACEHOLDER — e.g. assert haversine distance for known coords, temporal features,
and that serving-time features match training-time features for the same row.

Run:  pytest
"""
import pytest


@pytest.mark.skip(reason="TODO: implement once features/build_features.py is done")
def test_haversine_known_distance():
    ...
